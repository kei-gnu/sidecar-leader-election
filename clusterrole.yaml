apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: leader-election-cluster
  namespace: leader-election-test
  labels:
    app: leader-eelction-app
rules:
  - apiGroups: [""]
    resources: ["endpoints"]
    verbs: ["*"]
